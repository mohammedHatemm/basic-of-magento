<?xml version="1.0"?>
<schema xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:noNamespaceSchemaLocation="urn:magento:framework:Setup/Declaration/Schema/etc/schema.xsd">

    <table name="elshrif_loyalty_points" resource="default" engine="innodb"
           comment="Customer Loyalty Points Balance">

        <!-- Primary Key -->
        <column xsi:type="int" name="entity_id" nullable="false" identity="true"
                comment="Entity ID"/>

        <!-- Customer ID -   -->
        <column xsi:type="int" name="customer_id" nullable="false" unsigned="true"
                comment="Customer ID"/>

        <!-- Points columns -->
        <column xsi:type="int" name="points_balance" nullable="false" default="0"
                comment="Points Balance"/>
        <column xsi:type="int" name="total_earned" nullable="false" default="0"
                comment="Total Points Earned"/>
        <column xsi:type="int" name="total_spent" nullable="false" default="0"
                comment="Total Points Spent"/>
        <column xsi:type="timestamp" name="updated_at" on_update="true"
                nullable="false" default="CURRENT_TIMESTAMP" comment="Last Updated"/>

        <!-- Primary Key Constraint -->
        <constraint xsi:type="primary" referenceId="PRIMARY">
            <column name="entity_id"/>
        </constraint>

        <!-- Foreign Key -->
        <constraint xsi:type="foreign"
                    referenceId="ELSHRIF_LOYALTY_POINTS_CUSTOMER_ID"
                    table="elshrif_loyalty_points"
                    column="customer_id"
                    referenceTable="customer_entity"
                    referenceColumn="entity_id"
                    onDelete="CASCADE"/>

        <!-- Unique Constraint -->
        <constraint xsi:type="unique" referenceId="ELSHRIF_LOYALTY_POINTS_CUSTOMER_UNIQUE">
            <column name="customer_id"/>
        </constraint>

    </table>
</schema>
